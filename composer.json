{
    "name": "atlantpayments/paymentpage-sdk",
    "description": "Atlant payments SDK",
    "type": "library",
    "authors": [
        {
            "name": "Atlant Payment",
            "email": "sdk@atlantpayment.kz"
        }
    ],
    "autoload": {
        "psr-4": { "atlantpayments\\": "src/" }
    },
    "require": {
    },
    "require-dev": {
        "phpunit/phpunit": "^6.5||^7.2",
        "squizlabs/php_codesniffer": "^3.3"
    },
    "scripts": {
        "lint": "phpcs --report=full --report-width=120",
        "test": "phpunit --coverage-clover clover.xml"
    }
}
